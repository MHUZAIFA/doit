/** Default map panel height when not filling a flex parent (e.g. non-compact picker). */
export const MAP_PREVIEW_DEFAULT_CLASS =
  "z-0 h-[min(220px,40vh)] w-full overflow-hidden rounded-xs border border-border ring-1 ring-foreground/10"

/** Fills parent height (compact column); keeps a floor on small viewports. */
export const MAP_PREVIEW_FILL_CLASS =
  "z-0 h-full min-h-[200px] w-full overflow-hidden rounded-xs border border-border ring-1 ring-foreground/10"
